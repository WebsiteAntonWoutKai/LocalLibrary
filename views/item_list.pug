extends layout

block content
    h1 All Items

    form(action='', method='GET')
        div
            label Name: 
            | &nbsp
            input(type='text', name='name', value=`${searchOptions.name}`)
        div
            label Price limit: 0 
            | &nbsp
            input(type='range', min = "0", max = "100", name='price', value=`${searchOptions.price}` id="range")
            label 100 | &nbsp     
            <span id="value">#{searchOptions.price}</span>
        button(type='submit') Search
        script(src='/javascripts/slider.js')
    each item in items
        li
           | Name: #{item.name}
           | Price: #{item.price}
           | In stock: #{item.amountInStock}
           br
           a(href=item.url) 
            <img height="150" width="100" src="#{item.imagePath}">